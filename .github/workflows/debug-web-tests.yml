name: Deprecated web test diagnostic

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Temporary diagnostic is retired; use Web CI.'
