name: Deprecated one-time J-Quants refresh

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Use the secured refresh workflow
        run: |
          echo 'This temporary workflow no longer reads external credential files.'
          echo 'Configure a rotated JQUANTS_API_KEY repository secret and run jquants-screen.yml.'
