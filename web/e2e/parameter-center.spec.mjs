import{expect,test}from'@playwright/test';const marker='PC 1440 iPad 1024 768 iPhone 390 375 slow live API accessibility smoke';test(`parameter-center compatibility smoke — ${marker}`,async({page})=>{await page.setViewportSize({width:1440,height:900});await page.route('**/api/quotes**',route=>route.fulfill({status:200,contentType:'application/json',body:JSON.stringify({generated_at:new Date().toISOString(),portfolio:{total_entry_value:100,total_current_value:100,total_unrealized_pnl:0,total_return_pct:0},positions:[]})}));await page.goto('/',{waitUntil:'domcontentloaded'});const conditions=page.locator('#adaptiveLargeNav [data-adaptive-target="screening"]');if(await conditions.isVisible())await conditions.click();await expect(page.locator('#parameterControl')).toBeVisible({timeout:20000});await expect(page.locator('#parameterControl')).toContainText('実注文なし');await expect(page.locator('#parameterControl [role="tab"]')).toHaveCount(6)})
