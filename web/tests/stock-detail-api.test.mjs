import test from'node:test';import assert from'node:assert/strict';import{readFile}from'node:fs/promises';import{resolve}from'node:path';const root=resolve(import.meta.dirname,'..');test('stock detail API validates code, times out upstreams and returns partial sources',async()=>{const source=await readFile(resolve(root,'functions/api/stock-detail.js'),'utf8');assert.match(source,/normalizeStockCode/);assert.match(source,/FETCH_TIMEOUT_MS = 4000/);assert.match(source,/Promise\.allSettled/);assert.match(source,/unknown_code/);assert.match(source,/stale-while-revalidate=900/);assert.match(source,/Server-Timing/)});test('stock detail is lazy and not requested by initial scripts',async()=>{const app=await readFile(resolve(root,'stock-detail.js'),'utf8');assert.match(app,/\/api\/stock-detail\?code=/);const build=await readFile(resolve(root,'scripts/build.mjs'),'utf8');assert.doesNotMatch(build,/api\/stock-detail\?code=/)});
