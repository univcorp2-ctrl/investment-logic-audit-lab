import test from'node:test';import assert from'node:assert/strict';import{decisionLabel,decisionTone,reportRowsToCsv,summarizePlanImpact}from'../decision-report-core.js';test('paper decision labels are explicit',()=>{assert.equal(decisionLabel('SIM_BUY'),'買い候補');assert.equal(decisionLabel('SIM_SELL'),'売却候補');assert.equal(decisionTone('NO_DATA'),'nodata')});test('CSV separates fundamental and technical columns',()=>{const csv=reportRowsToCsv([{code:'1000',company_name:'例',decision:{action:'SIM_HOLD',confidence:55,reasons:['品質'],risks:['RSI']},holding:{quantity:100},fundamental:{score:60,value_score:70,quality_score:65},technical:{score:50,price:100,rsi14:72},quote:{valid:true,quote_time:'2026-08-03'}}]);assert.match(csv,/fundamental_score/);assert.match(csv,/technical_score/);assert.match(csv,/positive_reasons/);assert.doesNotMatch(csv,/api[_-]?key/i)});test('plan impact explains upgrade without promising profit',()=>{const impact=summarizePlanImpact([{id:'free',name:'Free'},{id:'standard',name:'Standard'}],'free');assert.equal(impact.current.name,'Free');assert.equal(impact.standard.name,'Standard');assert.match(impact.recommendation,/Standard/)})
