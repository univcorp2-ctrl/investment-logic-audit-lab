import test from'node:test';import assert from'node:assert/strict';import{filterAndSortRows,normalizePayload,rowsToCsv,summarizeRows}from'../dashboard.js';const payload=normalizePayload({metadata:{source:'J-Quants API V2',effective_data_cutoff:'2026-05-11',plan:{name:'free'}},rows:[{rank:2,code:'22220',company_name:'ベータ',market:'Standard',sector:'卸売業',overall_score:65,value_score:75,quality_score:60,technical_score:55,liquidity_score:70,value_trap_risk:20,data_completeness:80,eligible:true,positive_reasons:'割安 / 品質'},{rank:1,code:'11110',company_name:'アルファ',market:'Prime',sector:'電気機器',overall_score:82,value_score:80,quality_score:90,technical_score:72,liquidity_score:88,value_trap_risk:10,data_completeness:91,eligible:true,negative_reasons:'価格確認'},{rank:3,code:'33330',company_name:'ガンマ',market:'Growth',sector:'情報通信',overall_score:44,value_score:55,quality_score:30,technical_score:66,liquidity_score:40,value_trap_risk:75,data_completeness:45,eligible:false}]});test('normalizes J-Quants metadata and reasons',()=>{assert.equal(payload.isJQuants,true);assert.equal(payload.plan,'free');assert.deepEqual(payload.rows[0].positive_reasons,['割安','品質'])});test('filters and sorts deterministically',()=>{const result=filterAndSortRows(payload.rows,{minOverall:60,maxTrap:30,sortField:'quality_score',sortDirection:'desc',limit:30});assert.deepEqual(result.map(row=>row.code),['11110','22220'])});test('summaries and CSV contain sanitized display columns',()=>{const summary=summarizeRows(payload.rows);assert.equal(summary.total,3);assert.equal(summary.eligible,2);const csv=rowsToCsv(payload.rows.slice(0,1));assert.match(csv,/positive_reasons/);assert.doesNotMatch(csv,/api[_-]?key/i)});
