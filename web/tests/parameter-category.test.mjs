import test from 'node:test';import assert from'node:assert/strict';import{categoryCounts,parameterCategory}from'../parameter-category-core.js';test('parameters are visibly separated into investment pillars',()=>{assert.equal(parameterCategory('fundamental.minRoePct'),'Fundamental');assert.equal(parameterCategory('screening.minRsi'),'Technical');assert.equal(parameterCategory('screening.market'),'Universe');assert.equal(parameterCategory('risk.maxPortfolioDrawdownPct'),'Risk');assert.equal(parameterCategory('display.fontScale'),'Display')});test('category counts include every supported category',()=>{const counts=categoryCounts(['screening.market','fundamental.minRoePct','screening.minRsi','risk.maxPositionLossPct','display.fontScale']);assert.deepEqual(counts,{Universe:1,Fundamental:1,Technical:1,Risk:1,Display:1})});
