import test from'node:test';import assert from'node:assert/strict';import{calculatePortfolio,calculatePosition,money,percent}from'../demo-trade-core.js';const portfolio={positions:[{symbol:'AAA.T',code:'AAA',company_name:'A',quantity:100,entry_price:1000},{symbol:'BBB.T',code:'BBB',company_name:'B',quantity:100,entry_price:2000}]};test('calculates per-position unrealized result',()=>{const position=calculatePosition(portfolio.positions[0],{current_price:1100,usable:true,verification:'double-checked'});assert.equal(position.entry_value,100000);assert.equal(position.current_value,110000);assert.equal(position.unrealized_pnl,10000);assert.equal(position.return_pct,10)});test('aggregates portfolio and falls back to entry on unusable quote',()=>{const result=calculatePortfolio(portfolio,{generated_at:'2026-08-03T05:40:00Z',quotes:[{symbol:'AAA.T',current_price:1100,usable:true,verification:'double-checked'},{symbol:'BBB.T',current_price:1500,usable:false,verification:'price-discrepancy'}]});assert.equal(result.total_entry_value,300000);assert.equal(result.total_current_value,310000);assert.equal(result.total_unrealized_pnl,10000);assert.equal(result.winners,1);assert.equal(result.unchanged,1);assert.equal(result.verified_quotes,1)});test('formats Japanese currency and signed percent',()=>{assert.match(money(123456),/123,456/);assert.equal(percent(1.234),'+1.23%');assert.equal(percent(-1.234),'-1.23%')});
