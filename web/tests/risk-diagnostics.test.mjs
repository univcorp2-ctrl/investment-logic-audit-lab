import test from'node:test';import assert from'node:assert/strict';import{DEFAULT_RISK_POLICY,currentRiskSnapshot,evaluateRiskPolicy,mobileDateRange,normalizeRiskPolicy}from'../risk-diagnostics-core.js';test('user risk policy detects portfolio and position breaches',()=>{const metrics={sample:{seed_cost_basis:10000,current_equity:9000},series:{equity:[{date:'2026-08-03',equity:11000}]}};const quotes={portfolio:{total_current_value:9000,total_unrealized_pnl:-1000},positions:[{code:'1000',name:'A',entry_price:100,current_price:90,unrealized_pnl:-1000,return_pct:-10,usable:true}]};const demo={positions:[{code:'1000',company_name:'A',quantity:100,entry_price:100}]};const snapshot=currentRiskSnapshot(metrics,quotes,demo,{});const result=evaluateRiskPolicy(snapshot,{...DEFAULT_RISK_POLICY,maxPortfolioDrawdownPct:8,maxTotalUnrealizedLossPct:5,maxPositionLossPct:8});const codes=result.breaches.map(item=>item.code);assert.ok(codes.includes('portfolio_drawdown'));assert.ok(codes.includes('total_unrealized_pct'));assert.ok(codes.includes('position_pct:1000'))});test('normalization rejects negative limits',()=>{const policy=normalizeRiskPolicy({maxPositionLossPct:-1});assert.equal(policy.maxPositionLossPct,DEFAULT_RISK_POLICY.maxPositionLossPct)});test('mobile date range reports explicit start end and point count',()=>{const range=mobileDateRange([{date:'2026-08-03'},{date:'2026-08-07'}],'all');assert.match(range.start,/2026-08-03/);assert.match(range.end,/2026-08-07/);assert.equal(range.calendarDays,5);assert.equal(range.points,2)})
