import test from'node:test';import assert from'node:assert/strict';import{DEFAULT_CONFIG,parseCsv,scoreRows,summarizeRows,toCsv}from'../scoring.js';const rows=[{symbol:'GOOD',sector:'A',market_cap:100,enterprise_value:80,net_income:15,book_value:90,free_cash_flow:18,ebitda:20,dividends:3,buybacks:2,net_cash:30,revenue:120,gross_profit:60,operating_income:22,operating_cash_flow:20,total_assets:150,invested_capital:100,total_debt:8,revenue_growth:.12,eps_growth:.15,fcf_growth:.18,margin_stability:.9,earnings_volatility:.1,earnings_stability:.9,fcf_stability:.9,share_count_growth:-.02,debt_to_ebitda_change:-.2,operating_margin_change:.02,negative_earnings_years:0,negative_fcf_years:0,technical_score:72,risk_score:80,average_daily_value:1000},{symbol:'MID',sector:'A',market_cap:100,enterprise_value:120,net_income:8,book_value:50,free_cash_flow:5,ebitda:10,dividends:1,buybacks:0,net_cash:0,revenue:100,gross_profit:35,operating_income:10,operating_cash_flow:9,total_assets:150,invested_capital:110,total_debt:35,revenue_growth:.03,eps_growth:.02,fcf_growth:.01,margin_stability:.6,earnings_volatility:.3,earnings_stability:.6,fcf_stability:.5,share_count_growth:.01,debt_to_ebitda_change:.1,operating_margin_change:0,negative_earnings_years:0,negative_fcf_years:0,technical_score:52,risk_score:55,average_daily_value:500},{symbol:'TRAP',sector:'A',market_cap:100,enterprise_value:300,net_income:-10,book_value:15,free_cash_flow:-20,ebitda:3,dividends:0,buybacks:-4,net_cash:-90,revenue:80,gross_profit:10,operating_income:-8,operating_cash_flow:-5,total_assets:180,invested_capital:160,total_debt:130,revenue_growth:-.2,eps_growth:-.4,fcf_growth:-.5,margin_stability:.1,earnings_volatility:1.1,earnings_stability:.1,fcf_stability:.1,share_count_growth:.15,debt_to_ebitda_change:1.3,operating_margin_change:-.08,negative_earnings_years:3,negative_fcf_years:3,technical_score:35,risk_score:20,average_daily_value:20}];test('robust score ranks quality value above trap',()=>{const result=scoreRows(rows,DEFAULT_CONFIG);assert.equal(result[0].symbol,'GOOD');assert.ok(result.find(row=>row.symbol==='GOOD').overall_score>result.find(row=>row.symbol==='TRAP').overall_score);assert.equal(result.find(row=>row.symbol==='TRAP').eligible,false);assert.ok(result.find(row=>row.symbol==='TRAP').value_trap_risk>60)});test('missing metrics are not converted to zero',()=>{const result=scoreRows([{symbol:'A',market_cap:100},{symbol:'B',market_cap:200},{symbol:'C',market_cap:300}]);result.forEach(row=>{assert.ok(row.data_completeness<30);assert.ok(Number.isFinite(row.overall_score))})});test('weights must sum to 100',()=>assert.throws(()=>scoreRows(rows,{...DEFAULT_CONFIG,fundamentalWeight:50}),/100%/));test('csv parser and exporter preserve quoted values',()=>{const parsed=parseCsv('symbol,company_name\n7203,"トヨタ,自動車"\n');assert.equal(parsed[0].company_name,'トヨタ,自動車');assert.match(toCsv(parsed),/"トヨタ,自動車"/)});test('summary reports eligible and median',()=>{const summary=summarizeRows(scoreRows(rows));assert.equal(summary.total,3);assert.ok(summary.eligible>=1);assert.ok(summary.medianOverall>0)});
