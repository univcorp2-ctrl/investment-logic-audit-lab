import{cp,mkdir,readFile,rm,stat,writeFile}from'node:fs/promises';import{resolve}from'node:path';const root=resolve(import.meta.dirname,'..'),dist=resolve(root,'dist');await rm(dist,{recursive:true,force:true});await mkdir(dist,{recursive:true});for(const file of['index.html','styles.css','app.js','dashboard.js','scoring.js','demo-data.js','summary-fix.js','demo-trade.js','demo-trade-core.js','demo-trade.css','demo-portfolio.json'])await cp(resolve(root,file),resolve(dist,file));for(const file of['jquants-ranking.json','jquants-ranking.csv','live-ranking.json','live-ranking.csv']){try{await stat(resolve(root,file));await cp(resolve(root,file),resolve(dist,file))}catch{}}const indexPath=resolve(dist,'index.html');let index=await readFile(indexPath,'utf-8');for(const marker of['<link rel="stylesheet" href="./demo-trade.css" />'])if(!index.includes(marker))index=index.replace('</head>',`  ${marker}\n  </head>`);for(const marker of['<script type="module" src="./summary-fix.js"></script>','<script type="module" src="./demo-trade.js"></script>'])if(!index.includes(marker))index=index.replace('</body>',`  ${marker}\n  </body>`);await writeFile(indexPath,index,'utf-8');console.log('Built ValueScope Japan into web/dist');
